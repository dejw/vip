language: python
python:
    - "2.6"
    - "2.7"
install: 
    - pip install pep8
    - pip install -r requirements.txt --use-mirrors
    - pip install -e .
script:
    - python -m vip.main --version
    - pep8 vip
    - nosetests
